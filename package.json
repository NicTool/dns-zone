{
  "name": "dns-zone-validator",
  "version": "0.5.0",
  "description": "DNS Zone Validator",
  "main": "index.js",
  "scripts": {
    "grammar": "npx -p nearley nearleyc src/zonefile-grammar.ne -o dist/zonefile-grammar.js",
    "lint": "npx eslint index.js test/*.js",
    "lintfix": "npx eslint --fix index.js test/*.js",
    "postinstall": "npx -p nearley nearleyc src/zonefile-grammar.ne -o dist/zonefile-grammar.js",
    "test": "npx mocha",
    "versions": "npx dependency-version-checker check"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/msimerson/dns-zone-validator.git"
  },
  "keywords": [
    "DNS",
    "zone",
    "validator",
    "validation"
  ],
  "author": "Matt Simerson <matt@tnpi.net>",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/msimerson/dns-zone-validator/issues"
  },
  "homepage": "https://github.com/msimerson/dns-zone-validator#readme",
  "devDependencies": {
    "eslint": "^8.11.0",
    "mocha": "^9.2.2"
  },
  "dependencies": {
    "command-line-args": "^5.2.1",
    "command-line-usage": "^6.1.1",
    "dns-resource-record": "^0.9.4",
    "nearley": "^2.20.1"
  }
}

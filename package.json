{
  "name": "dns-zone-validator",
  "version": "0.1.0",
  "description": "DNS Zone Validator",
  "main": "index.js",
  "scripts": {
    "cover": "NODE_ENV=cov npx nyc --reporter=lcovonly npm run test",
    "grammar": "npx -p nearley nearleyc src/bind-grammar.ne -o grammar.js",
    "lint": "npx eslint index.js test/*.js",
    "lintfix": "npx eslint --fix index.js test/*.js",
    "postinstall": "npx -p nearley nearleyc src/bind-grammar.ne -o grammar.js",
    "test": "npx mocha --exit"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/msimerson/dns-zone-validator.git"
  },
  "keywords": [
    "DNS",
    "zone",
    "validator",
    "validation"
  ],
  "author": "Matt Simerson <matt@tnpi.net>",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/msimerson/dns-zone-validator/issues"
  },
  "homepage": "https://github.com/msimerson/dns-zone-validator#readme",
  "devDependencies": {
    "eslint": "^8.0.1",
    "mocha": "^9.1.3"
  },
  "dependencies": {
    "nearley": "^2.20.1"
  }
}
